<Window x:Class="Library.View.BookView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library.View"
        xmlns:localUC="clr-namespace:Library"
        mc:Ignorable="d"
        Title="BookView" Height="250" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="6*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <DockPanel  Background="Gray" Grid.Row="0"  VerticalAlignment="Stretch"
HorizontalAlignment="Stretch"
                    Grid.ColumnSpan="2" >

        </DockPanel>
        <DockPanel   Background="LightGray" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                     Grid.ColumnSpan="2" >

            <StackPanel>
                <WrapPanel>
                    <TextBlock HorizontalAlignment="Left"  Text="Name" Width="100" Height="30"    >

                    </TextBlock>
                    <TextBox Height="30" Width="100 " Text="{Binding CurrentBook.Name,Mode=TwoWay}"  Name="Name" >
                    </TextBox>
                </WrapPanel>
                <WrapPanel>
                    <TextBlock HorizontalAlignment="Left"  Text="Author" Width="100" Height="30"    ></TextBlock>
                    <TextBox Height="30" Width="100" Text="{Binding CurrentBook.Author,Mode=TwoWay}"  Name="Author"></TextBox>
                </WrapPanel>
                <WrapPanel>
                    <TextBlock HorizontalAlignment="Left" Text="Count"   Width="100" Height="30"></TextBlock>
                    <TextBox Height="30" Width="100"  Name="Count" Text="{Binding CurrentBook.Count,Mode=TwoWay}"></TextBox>
                </WrapPanel>
            </StackPanel>
            <StackPanel>

            </StackPanel>
            <StackPanel>
                <WrapPanel>
                </WrapPanel>

            </StackPanel>
            <StackPanel>
                <TextBlock Margin="100,0,0,0" Height="30" VerticalAlignment="Top" Text="Filiali" Width="100" ></TextBlock>
                <TextBlock Margin="100,0,0,0" Height="30" VerticalAlignment="Top" Text="SalePrice" Width="100" ></TextBlock>
                <TextBlock Margin="100,0,0,0" Height="30" VerticalAlignment="Top" Text="BuyPrice" Width="100" ></TextBlock>

            </StackPanel>
            <StackPanel>
                <ComboBox Name="Filial" Height="30" Width="100" ItemsSource="{Binding filials}" DisplayMemberPath="Name"
                              SelectedItem="{Binding BookViewModel.Filial }" SelectedValuePath="Id"
                              SelectedValue="{Binding BookViewModel.Filial.Id ,Mode=TwoWay}"></ComboBox>
                <TextBox Height="30" Name="SalePrice" VerticalAlignment="Top" Width="100 " Text="{Binding CurrentBook.SalePrice,Mode=TwoWay}">
                </TextBox>
                <TextBox Height="30" Name="ByPrice" VerticalAlignment="Top" Width="100 " Text="{Binding CurrentBook.BuyPrice,Mode=TwoWay}">
                </TextBox>
            </StackPanel>
            <StackPanel>
                <Button Height="30" Width="95"   HorizontalAlignment="Right" VerticalAlignment="Top" Command="{Binding addBook}" Content="Add" Name="Add" >
                    <!--<Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text.Length, ElementName=QrupAdi, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Text.Length, ElementName=Seviyye, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Text.Length, ElementName=Filial, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Text.Length, ElementName=XidmetNovu, UpdateSourceTrigger=PropertyChanged}" Value="0">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                                <DataTrigger  Binding="{Binding state, Mode=TwoWay}" Value="2">
                                    <Setter Property="IsEnabled" Value="False"></Setter>
                                </DataTrigger>
                            </Style.Triggers >
                            <Setter Property="IsEnabled" Value="True"></Setter>

                        </Style >

                    </Button.Style>-->



                </Button>
                <Button Name="Update" Height="30" Width="95" HorizontalAlignment="Right" Command="{Binding Update}" VerticalAlignment="Top" Content="Deyis" >
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Style.Triggers>
                                <DataTrigger  Binding="{Binding state, Mode=TwoWay}" Value="2">
                                    <Setter Property="IsEnabled" Value="True"></Setter>
                                </DataTrigger>
                            </Style.Triggers >
                            <Setter Property="IsEnabled" Value="False"></Setter>
                        </Style >
                    </Button.Style>
                </Button>
                <Button Height="30" Width="95" HorizontalAlignment="Right" Command="{Binding imtina}" VerticalAlignment="Top" Content="Delete"  >
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding state, Mode=TwoWay}" Value="2">
                                    <Setter Property="IsEnabled" Value="True"></Setter>
                                </DataTrigger>
                            </Style.Triggers>
                            <Setter Property="IsEnabled" Value="True"></Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

        </DockPanel>

        <TextBlock  Height="30" Text="Qruplar" FontWeight="Bold" FontFamily="30"  Width="100" HorizontalAlignment="Center" Grid.ColumnSpan="2" Margin="246,0,246,1">
        </TextBlock>
     
        <ListView Visibility="Hidden" Name="List"   SelectedItem="{Binding SelectBook }" ItemsSource="{Binding Books}" Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.ColumnSpan="2">

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="No" Width="30" >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Padding="5,0" Text="{Binding Id}" TextAlignment="Center" VerticalAlignment="Center">
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Name" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Padding="5,0" Text="{Binding Name}" TextAlignment="Center" VerticalAlignment="Center">
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Author" Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Padding="5,0" Text="{Binding Author}"   TextAlignment="Center" VerticalAlignment="Center">
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>

                    </GridViewColumn>
                    <GridViewColumn Header="Count" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Padding="5,0" Text="{Binding Count}" TextAlignment="Center" VerticalAlignment="Center">
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Filial" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Padding="5,0" Text="{Binding BookViewModel.Filial}"  TextAlignment="Center" VerticalAlignment="Center">
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>

                    </GridViewColumn>
                    <GridViewColumn Header="SalePrice" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Padding="5,0" Text="{Binding SalePrice}" TextAlignment="Center" VerticalAlignment="Center">
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="ByPrice" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Padding="5,0" Text="{Binding ByPrice}" TextAlignment="Center" VerticalAlignment="Center">
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>


                </GridView>


            </ListView.View>
        </ListView>


    </Grid>
</Window>
